#!/usr/bin/env bash

set -e

#=================================
# Colors
#=================================

RESET=$(tput sgr0)
BOLD=$(tput bold)

GREEN=$(tput setaf 2)
CYAN=$(tput setaf 6)
YELLOW=$(tput setaf 3)
RED=$(tput setaf 1)
GRAY=$(tput setaf 8)
BLUE=$(tput setaf 4)
WHITE=$(tput setaf 7)


#=================================
# System Information
#=================================

USER_NAME="${USER}"
HOST_NAME="$(hostname)"
DISTRO="$(grep '^PRETTY_NAME=' /etc/os-release | cut -d= -f2 | tr -d '"')"
KERNEL="$(uname -r)"
DESKTOP="${XDG_CURRENT_DESKTOP:-Unknown}"
CPU="$(grep -m1 'model name' /proc/cpuinfo | cut -d: -f2 | xargs)"

#=================================
# APT Guard
#=================================

if ! command -v apt >/dev/null 2>&1; then
    echo ""
    echo "${YELLOW}Not applicable to ${DISTRO}.${RESET}"
    echo "${YELLOW}APT package manager not found.${RESET}"
    echo ""
    exit 0
fi

#=================================
# Banner
#=================================

# clear

echo "${WHITE}"
echo -e "      ████  ██████ ████  █  ████  ██████      "
echo -e "      █   █ █      █   █ █  █   █ █    █      ${GRAY}"
echo -e "      ██  █ ███    █████ ██ █████ ██   █      ${CYAN}"
echo -e "      ██  █ ██     ██  █ ██ ██  █ ██   █      ${BLUE}"
echo -e "      █████ ██████ █████ ██ ██  █ ██   █      ${WHITE}"
echo -e "                                              "
echo -e " █    █ █████ ████  ████  █████ ██████ █████  "
echo -e " █    █ █   █ █   █ █   █   █   █      █   █  ${GRAY}"
echo -e " ██   █ █████ ██  █ █████   ██  ███    ██████ ${CYAN}"
echo -e " ██   █ ██    ██  █ ██  █   ██  ██     ██   █ ${BLUE}"
echo -e " ██████ ██    █████ ██  █   ██  ██████ ██   █ ${WHITE}"
echo ""

echo ""
echo "${GREEN}≣ Debian Workstation Updater${RESET}"
echo ""

echo "${GREEN}User:${RESET}    ${USER_NAME}"
echo "${GREEN}Host:${RESET}    ${HOST_NAME}"
echo "${GREEN}Distro:${RESET}  ${DISTRO}"
echo "${GREEN}Kernel:${RESET}  ${KERNEL}"
echo "${GREEN}Desktop:${RESET} ${DESKTOP}"
echo "${GREEN}CPU:${RESET}     ${CPU}"
echo ""

#=================================
# Refresh Metadata
#=================================

echo "${GREEN}≣ Refreshing Package Metadata${RESET}"
sudo -v
sudo apt update

#=================================
# Update Counts
#=================================

APT_UPDATES=$(apt list --upgradable 2>/dev/null | tail -n +2 | wc -l)

FLATPAK_UPDATES=0
if command -v flatpak >/dev/null 2>&1; then
    FLATPAK_UPDATES=$(flatpak remote-ls --updates 2>/dev/null | wc -l)
fi

SNAP_UPDATES=0
if command -v snap >/dev/null 2>&1; then
    SNAP_UPDATES=$(snap refresh --list 2>/dev/null | tail -n +2 | wc -l)
fi

echo ""
echo "${GREEN}APT Updates:${RESET}      ${APT_UPDATES}"
echo "${GREEN}Flatpak Updates:${RESET}  ${FLATPAK_UPDATES}"
echo "${GREEN}Snap Updates:${RESET}     ${SNAP_UPDATES}"
echo ""

TOTAL_UPDATES=$((APT_UPDATES + FLATPAK_UPDATES + SNAP_UPDATES))

if (( TOTAL_UPDATES == 0 )); then
    echo "${GREEN}System is already up to date.${RESET}"
    exit 0
fi

#=================================
# Updates
#=================================

echo "${GREEN}≣ Updating APT Packages${RESET}"
sudo apt full-upgrade -y
sudo apt autoremove -y
sudo apt autoclean -y

if command -v flatpak >/dev/null 2>&1; then
    echo ""
    echo "${GREEN}≣ Updating Flatpaks${RESET}"
    flatpak update -y
fi

if command -v snap >/dev/null 2>&1; then
    echo ""
    echo "${GREEN}≣ Updating Snaps${RESET}"
    sudo snap refresh
fi

#=================================
# Reboot Check
#=================================

echo ""

if [[ -f /var/run/reboot-required ]]; then
    echo "${YELLOW}Reboot required.${RESET}"
else
    echo "${GREEN}No reboot required.${RESET}"
fi

echo ""
echo "${GREEN}Update complete.${RESET}"
